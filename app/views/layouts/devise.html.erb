<!DOCTYPE html>
<html lang="en">
  <%= render 'head' %>
<body>
  <%= render 'static-navbar' %>
  <%= render 'flashes' %>
  <div class="container">
    <div class="row">
      <div class="col-12 col-lg-6 offset-lg-3">
        <%= yield %>
      </div>
    </div>
  </div>
  <%= render 'javascript' %>
  <%= render 'css_overrides' %>
</body>
</html>
